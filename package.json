{
  "name": "setu",
  "private": true,
  "version": "1.0.0",
  "description": "SETU - Scheme Eligibility Contradiction Agent",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:full": "concurrently -n ml,server,client -c yellow,blue,green \"npm run dev:ml\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:ml": "cd ml && uvicorn serve:app --port 8001",
    "seed": "npm run seed --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
